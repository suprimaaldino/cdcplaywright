name: Playwright Tests

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    strategy:
      matrix:
        shard: [1, 2, 3]
      fail-fast: false

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright Browsers
        run: npx playwright install --with-deps

      - name: Run Playwright tests (shard ${{ matrix.shard }})
        run: npx playwright test --shard=${{ matrix.shard }}/3
        env:
          CI: true
          TEST_SHARD: ${{ matrix.shard }}

      - name: Upload test report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report-${{ matrix.shard }}
          path: playwright-report/
          retention-days: 7

  # visual-test:
  #   needs: test
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4

  #     - uses: actions/setup-node@v4
  #       with:
  #         node-version: '20'
  #         cache: 'npm'

  #     - name: Install dependencies
  #       run: npm ci

  #     - name: Install Playwright Browsers
  #       run: npx playwright install --with-deps

  #     - name: Run visual regression tests
  #       run: |
  #         if [[ ${{ github.event_name }} == 'push' && ${{ github.ref }} == 'refs/heads/main' ]]; then
  #           npx playwright test tests/visualTest/ --update-snapshots
  #           # Commit updated snapshots for main branch
  #           git config --global user.name "GitHub Actions"
  #           git config --global user.email "actions@github.com"
  #           git add tests/visualTest/*-snapshots/
  #           git commit -m "Update visual regression snapshots [skip ci]" || echo "No snapshot changes to commit"
  #           git push
  #         else
  #           npx playwright test tests/visualTest/
  #         fi
  #       env:
  #         GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  #     - name: Upload visual report
  #       if: always()
  #       uses: actions/upload-artifact@v4
  #       with:
  #         name: visual-test-report
  #         path: |
  #           playwright-report/
  #           tests/visualTest/*-snapshots/
  #         retention-days: 7

  merge-report:
    needs: test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Download all shard reports
        uses: actions/download-artifact@v4
        with:
          path: playwright-reports
          pattern: playwright-report-*
          merge-multiple: true

      - name: Install merge tool
        run: npm install -D playwright-merge-html-reports

      - name: Merge Playwright reports
        run: npx playwright-merge-html-reports --input playwright-reports --output merged-report

      - name: Upload merged report
        uses: actions/upload-artifact@v4
        with:
          name: merged-playwright-report
          path: merged-report/

  deploy-report:
    needs: merge-report
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    steps:
      - uses: actions/checkout@v4

      - name: Download merged report
        uses: actions/download-artifact@v4
        with:
          name: merged-playwright-report
          path: merged-report

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: merged-report/